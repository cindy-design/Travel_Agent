# 应用配置
APP_NAME=LX SkyRoam Agent
VERSION=1.0.0
DEBUG=false

# 服务器配置
HOST=0.0.0.0
PORT=8001
ALLOWED_HOSTS=["*"]

# 数据库配置
DATABASE_URL=postgresql://postgres:123456@localhost:5432/skyroam

# Redis配置
REDIS_URL=redis://localhost:6379/0
REDIS_PASSWORD=

# Celery配置
# 若用官方云，Celery 对 Redis 的 rediss:// URL 有强校验，必须带 ssl_cert_reqs 参数，且值为 CERT_REQUIRED 、 CERT_OPTIONAL 或 CERT_NONE ，否则抛错。
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2
# 设置池类型（ solo 、 threads 、 prefork ）
CELERY_WORKER_POOL=threads
# 并发数（整数）
CELERY_WORKER_CONCURRENCY=2
# 控制队列预取数（每个并发槽预先从队列取多少任务）
CELERY_PREFETCH_MULTIPLIER=2

# SSE推送间隔秒
PLAN_STATUS_STREAM_INTERVAL=2
# SSE最大推送时长秒
PLAN_STATUS_STREAM_MAX_SECONDS=900

# 小红书服务配置
XHS_API_BASE=http://127.0.0.1:8002

# OpenAI配置
OPENAI_API_KEY=sk-1111
OPENAI_MODEL=glm-4-flash
OPENAI_MAX_TOKENS=65536
OPENAI_TEMPERATURE=0.7
OPENAI_API_BASE=https://open.bigmodel.cn/api/paas/v4
OPENAI_TIMEOUT=300
OPENAI_MAX_RETRIES=3

# 第三方API配置
WEATHER_API_KEY=your-openweathermap-api-key
HOTEL_API_KEY=your-booking-api-key
MAP_API_KEY=your-google-maps-api-key

# Amadeus API配置
AMADEUS_CLIENT_ID=1111
AMADEUS_CLIENT_SECRET=1111
AMADEUS_API_BASE=https://test.api.amadeus.com
AMADEUS_TOKEN_URL=https://test.api.amadeus.com/v1/security/oauth2/token



# 地图服务提供商配置
MAP_PROVIDER=amap  # 可选值: "baidu" 或 "amap"

# 地图服务回退顺序（逗号分隔）
MAP_PROVIDER_FALLBACK=amap,baidu,tianditu

# 地点输入框提示配置
MAP_INPUT_TIPS_ENABLED=true  # 输入提示开关
MAP_TIPS_RATE_LIMIT_MAX=10  # 每 IP 每窗口最大次数
MAP_TIPS_RATE_LIMIT_WINDOW=60  # 限流窗口秒数
MAP_TIPS_CACHE_TTL=60  # 提示缓存 TTL 秒
MAP_CACHE_ENABLED=true  # 地图缓存开关

# 高德地图 API 密钥
AMAP_API_KEY=1111

# 百度地图 API 密钥
BAIDU_MAPS_API_KEY=1111

# 高德地图 MCP 服务配置
AMAP_MCP_MODE=http  # 可选值: "http" 或 "sse"
AMAP_MCP_HTTP_URL=http://localhost:3002/mcp
AMAP_MCP_SSE_URL=http://localhost:3002/sse

# 天地图API配置
TIANDITU_API_KEY=your_tianditu_api_key
TIANDITU_API_BASE=https://api.tianditu.gov.cn

RESTAURANT_DATA_SOURCE=amap  # 可选值: "amap" 或 "baidu" 或 "both"

# 天气数据源配置
WEATHER_DATA_SOURCE=amap  # 可选值: "amap" 或 "baidu" 或 "openweather"

# 小红书爬虫并发
XHS_MAX_CONCURRENT_CRAWLERS=2

# 安全配置
SECRET_KEY=lxai20251010
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=360

# 文件存储
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=logs/app.log
LOG_MAX_SIZE=10 MB
LOG_RETENTION=5
LOG_COMPRESSION=zip
LOG_TO_CONSOLE=true
LOG_TO_FILE=true

# 缓存配置
CACHE_TTL=3600
CACHE_MAX_SIZE=1000

# 任务配置
TASK_TIMEOUT=300
MAX_CONCURRENT_TASKS=10

# 限流配置
RATE_LIMIT_ENABLED=true
# 窗口内最大请求数
RATE_LIMIT_MAX_REQUESTS=60
# 窗口时长（秒）
RATE_LIMIT_WINDOW_SECONDS=10
# 白名单 IP 列表，默认空
RATE_LIMIT_WHITELIST=[]
# 排除路径列表
RATE_LIMIT_EXCLUDE_PATHS=["/docs","/redoc","/openapi.json"]
